<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: rgb(156, 239, 255);
        }
    </style>
</head>

<body>
    <div>
        <div>
            <a href="/prodRoute/getData">home</a>
            <a href="/prodRoute/form">form</a>
            <a href="/authRoute/registerPage">signup</a>
            <a href="/authRoute/loginPage">login</a>
        </div>

        <h1>Generate Otp </h1>
        <form id="resetForm">
            <div>
                <label for="email">Enter email</label> <br>
                <input type="text" id="email" name="otp" required placeholder="Entere your eamil">
            </div>

            <br>
            <div>
                <button type="submit">Generare Otp </button>
            </div>
        </form>
    </div>
    <script>


        let resetForm = document.querySelector("#resetForm");
        
        resetForm.addEventListener('submit', (e) => {
            e.preventDefault();

            let email = document.querySelector("#email").value;

            fetch("/authRoute/generateOtp", {
                method: "POST",
                body: JSON.stringify({ email }),
                headers: {
                    "Content-Type": "application/json"
                }
            })
                .then((res) => {
                    res.json()

                })
                .then((data) => {
                    window.location.href = "/authRoute/verifyOtp";
                    console.log(data, "otp generated");
                })
                .catch((error) => console.log(error));

            window.location.href = "/authRoute/verifyOtp";
        })
    </script>

</body>

</html>